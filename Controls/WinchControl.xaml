<UserControl x:Class="SealenceDemo.Controls.WinchControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SealenceDemo.Controls"
             xmlns:vm="clr-namespace:SealenceDemo.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:WinchViewModel/>
    </UserControl.DataContext>
    
    <Grid Width="120" Height="100">
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="{Binding Title}" HorizontalAlignment="Center"
                   FontWeight="Bold"/>
        <Grid Grid.Row="1" HorizontalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="60"/>
                <ColumnDefinition Width="60"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="Payed out" HorizontalAlignment="Left"/>
                <TextBlock Text="Load" HorizontalAlignment="Left"/>
                <TextBlock Text="Speed" HorizontalAlignment="Left"/>
            </StackPanel>

            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding PayedOutText}" HorizontalAlignment="Left" Margin="5,0,0,0"/>
                <TextBlock Text="{Binding LoadText}" HorizontalAlignment="Left" Foreground="Blue" Margin="5,0,0,0"/>
                <TextBlock Text="{Binding SpeedText}" HorizontalAlignment="Left" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
